/// In-app toast after an achievement unlock (from Edge `unlocks` array).
class AchievementUnlockToast {
  final String line;
  final String? meta;

  const AchievementUnlockToast({required this.line, this.meta});
}
